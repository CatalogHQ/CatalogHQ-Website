# Frontend (Vite)
# Production must use the API backend.
VITE_USE_API=true
# Used for API requests and build-time CSP connect-src (see scripts/sync-deploy-headers.mjs).
VITE_API_URL=https://api.cataloghq.store

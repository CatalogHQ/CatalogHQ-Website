# Set to true to use the NestJS backend instead of localStorage
VITE_USE_API=false

# In dev with Vite proxy, use /api (see vite.config.ts)
# For direct API access: http://localhost:3001
VITE_API_URL=/api
